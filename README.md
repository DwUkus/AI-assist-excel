# AI Ассистент для Excel

🤖 Office Add-in для автоматизации работы с Excel через текстовые команды на естественном языке с использованием AI (LLM через OpenRouter).

## 🎯 Возможности

- **Естественный язык** — общайтесь с Excel как с ассистентом
- **Умные команды** — перемещение, копирование, форматирование данных
- **Контекст таблицы** — AI видит ваши данные и понимает что делать
- **История чатов** — несколько сессий с сохранением истории
- **Undo (ограниченно)** — отмена последних действий (если доступен обратный шаг)
- **Поддержка русского языка** — полная локализация интерфейса

## 📋 Поддерживаемые команды

### Работа с ячейками
| Команда | Описание |
|---------|----------|
| `SET_VALUE\|A1\|Текст` | Установить значение в ячейку |
| `SET_FORMULA\|A1\|=SUM(B1:B10)` | Установить формулу |
| `FORMAT_BOLD\|A1\|true` | Жирный шрифт |
| `FORMAT_COLOR\|A1\|red` | Цвет заливки (alias) |
| `FORMAT_FILL\|A1\|#FF0000` | Цвет заливки |
| `CLEAR\|A1:D10` | Очистить диапазон |
| `SELECT\|A1:D10` | Выделить диапазон |

### Работа со строками/столбцами
| Команда | Описание |
|---------|----------|
| `DELETE_ROWS\|5:5` | Удалить строку 5 |
| `INSERT_ROWS\|5:5` | Вставить строку перед 5 |
| `COPY_ROW\|5\|10` | Скопировать строку 5 в 10 |
| `MOVE_ROW\|5\|10\|after` | Переместить строку 5 после строки 10 |
| `SWAP_ROWS\|3\|7` | Поменять строки 3 и 7 местами |
| `DELETE_COLUMNS\|B:B` | Удалить столбец B |
| `INSERT_COLUMNS\|B:B` | Вставить столбец перед B |

### Работа с листами
| Команда | Описание |
|---------|----------|
| `CREATE_SHEET\|Отчёт` | Создать новый лист |
| `DELETE_SHEET\|Лист1` | Удалить лист |
| `RENAME_SHEET\|Новое имя` | Переименовать активный лист |
| `ACTIVATE_SHEET\|Лист2` | Переключиться на лист |

### Диаграммы
| Команда | Описание |
|---------|----------|
| `CREATE_CHART\|ColumnClustered\|A1:D10\|Продажи` | Создать диаграмму |
| `DELETE_CHART\|Продажи` | Удалить диаграмму |
| `DELETE_ALL_CHARTS` | Удалить все диаграммы |

### Прочее
| Команда | Описание |
|---------|----------|
| `COPY\|A1:D10\|Sheet2!A1` | Копировать диапазон |
| `SORT\|A1:D10\|0\|true` | Сортировка (колонка 0, по возрастанию) |
| `UNDO` | Отменить последнее действие |

## 🚀 Установка и запуск

### Требования
- Node.js 18+
- npm или yarn
- Microsoft Excel (Desktop для Windows/Mac или Excel Online)
- Аккаунт [OpenRouter](https://openrouter.ai) для получения API ключа

### Быстрый старт (5 минут)

#### 1. Клонируйте и установите зависимости
```bash
git clone https://github.com/DwUkus/AI-assist-excel.git
cd AI-assist-excel/excel-addin
npm install
```

#### 2. Запустите dev-сервер
```bash
npm run dev-server
```
Сервер запустится на `https://localhost:3000`. При первом запуске система предложит установить SSL сертификат — примите его.

#### 3. Загрузите надстройку в Excel

**Вариант A: Автоматически (Windows с Excel Desktop)**
```bash
# В новом терминале
npm start
```
Эта команда автоматически откроет Excel и загрузит надстройку.

**Вариант B: Вручную (Excel Online или Mac)**

1. Откройте Excel Online или Excel Desktop
2. Перейдите: **Вставка** → **Надстройки** → **Мои надстройки**
3. Выберите **Отправить мою надстройку** (Upload My Add-in)
4. Загрузите файл `manifest.xml` из папки `excel-addin/`
5. Надстройка появится на вкладке **Главная** (Home)

#### 4. Настройте API ключ

1. На вкладке **Главная** нажмите кнопку **AI Ассистент**
2. В открывшемся диалоге нажмите **Настройки** (⚙️)
3. Вставьте ваш [OpenRouter API Key](https://openrouter.ai/keys)
4. Нажмите **Сохранить**

#### 5. Готово! Начните работу

Выделите данные в таблице и напишите команду, например:
- *"Удали пустые строки"*
- *"Отсортируй по второму столбцу"*
- *"Сделай заголовки жирными"*

---

### Сборка для продакшена

```bash
npm run build
```
Результат будет в папке `dist/`.

## ⚙️ Настройка

### Получение API ключа OpenRouter

1. Зарегистрируйтесь на [openrouter.ai](https://openrouter.ai)
2. Перейдите в [Keys](https://openrouter.ai/keys)
3. Нажмите **Create Key**
4. Скопируйте ключ (начинается с `sk-or-...`)

> 💡 **Бесплатные модели:** OpenRouter предоставляет бесплатный доступ к некоторым моделям (Gemini, Llama, DeepSeek). Кредитная карта не требуется.

### Настройка надстройки

1. Откройте надстройку в Excel
2. Нажмите **Настройки** (⚙️)
3. Введите **API Key** от [OpenRouter](https://openrouter.ai)
4. Выберите модель (по умолчанию доступны бесплатные модели)

### Поддерживаемые модели (по умолчанию)
- StepFun 3.5 Flash (Free)


Можно добавить любую модель из [каталога OpenRouter](https://openrouter.ai/models).

## 🏗️ Архитектура

```
excel-addin/
├── src/
│   ├── taskpane/          # Главная панель надстройки
│   │   └── taskpane.ts    # Точка входа, открытие диалога
│   ├── dialog/            # AI чат-диалог
│   │   └── dialog.ts      # UI чата, интеграция с LLM
│   ├── services/
│   │   ├── OpenRouterService.ts  # API клиент для LLM
│   │   ├── CommandParser.ts      # Парсер DSL команд
│   │   ├── CommandExecutor.ts    # Исполнитель команд Excel
│   │   └── ContextBuilder.ts     # Сбор контекста из Excel
│   └── commands/          # Ribbon команды
├── assets/                # Иконки
├── manifest.xml           # Манифест Office Add-in
└── package.json
```

### Поток данных

```
User Input → Dialog.ts → OpenRouterService → LLM
                ↓
         CommandParser (DSL → Commands)
                ↓
         CommandExecutor (Commands → Excel API)
                ↓
         Excel Workbook
```

## 🔧 Скрипты npm

| Команда | Описание |
|---------|----------|
| `npm start` | Запустить Excel с надстройкой |
| `npm run dev-server` | Запустить webpack dev server |
| `npm run build` | Production сборка |
| `npm run build:dev` | Development сборка |
| `npm run watch` | Режим наблюдения за изменениями |
| `npm run lint` | Проверка кода ESLint |
| `npm run lint:fix` | Исправление проблем линтера |
| `npm run prettier` | Форматирование кода |
| `npm run validate` | Валидация манифеста |
| `npm run deploy` | Публикация на GitHub Pages |
| `npm run signin` | Вход в M365 для отладки |
| `npm run signout` | Выход из M365 |

## 📝 Примеры использования

### Пример 1: Форматирование
> "Сделай заголовки в первой строке жирными и покрась их в синий"

AI выполнит:
```
FORMAT_BOLD|1:1|true
FORMAT_COLOR|1:1|blue
```

### Пример 2: Работа с данными
> "Удали все строки где в колонке B написано 'Удалить'"

AI проанализирует данные и выполнит:
```
DELETE_ROWS|5:5
DELETE_ROWS|12:12
```

### Пример 3: Создание диаграммы
> "Построй столбчатую диаграмму по данным A1:D10 с названием 'Продажи'"

```
CREATE_CHART|ColumnClustered|A1:D10|Продажи
```

## 🔐 Безопасность

- API ключ хранится только в localStorage браузера
- Данные Excel отправляются в OpenRouter только если включен переключатель **📝 Данные**
- Надстройка работает локально (localhost:3000)

## ❓ Устранение проблем

### Надстройка не загружается
- Убедитесь что dev-сервер запущен (`npm run dev-server`)
- Проверьте что сертификат принят браузером (откройте https://localhost:3000)
- Попробуйте удалить и заново загрузить манифест

### "API Key is missing"
- Откройте настройки (⚙️) и введите ключ от OpenRouter
- Ключ должен начинаться с `sk-or-`

### Команды не выполняются
- Убедитесь что выделены нужные данные в таблице
- Включите чекбокс "📝 Данные" для отправки контекста AI
- Попробуйте переформулировать запрос более конкретно

## 📦 Деплой на GitHub Pages

Подробная инструкция для публикации и загрузки манифеста: [excel-addin/deployment_guide.md](excel-addin/deployment_guide.md)

### Ошибка SSL сертификата
```bash
# Перегенерируйте сертификаты
npx office-addin-dev-certs install --machine
```

## 📄 Лицензия

MIT

## 🤝 Контрибьюция

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request
