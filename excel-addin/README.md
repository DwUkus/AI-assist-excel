# AI Excel Assistant

**AI Excel Assistant** — это надстройка для Microsoft Excel, которая позволяет выполнять операции с таблицами используя естественный язык. Проект использует возможности искусственного интеллекта (через OpenRouter) для интерпретации команд пользователя и автоматизации рутинных задач в Excel.

## 🚀 Возможности

- **Голосовые и текстовые команды**: Управляйте Excel через чат-интерфейс.
- **Интеллектуальная обработка**: Понимание контекста и намерений пользователя.
- **Автоматизация задач**: Форматирование, создание формул, анализ данных, построение графиков (в зависимости от реализованных навыков).
- **Поддержка русского языка**: Интерфейс и команды полностью локализованы.

## 🛠 Технологический стек

- **Frontend**: HTML, CSS, TypeScript
- **Сборка**: Webpack
- **Платформа**: Office.js (Excel JavaScript API)
- **AI Integration**: OpenRouter API

## 📋 Предварительные требования

- **Node.js**: Версия 16 или выше.
- **NPM**: Пакетный менеджер (обычно устанавливается вместе с Node.js).
- **Excel**: Microsoft Excel 2016 или новее (Desktop, Web или Mac).

## ⚙️ Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/DwUkus/AI-assist-excel.git
cd excel-addin
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Запуск в режиме разработки

Для запуска локального сервера разработки и автоматической загрузки надстройки в Excel (Desktop):

```bash
npm start
```

Эта команда:

1.  Запустит `webpack-dev-server`.
2.  Откроет Excel.
3.  Загрузит манифест надстройки (`manifest.xml`) в режиме sideloading.

> **Примечание**: Вам может потребоваться подтвердить установку сертификата разработчика при первом запуске.

## 📦 Сборка и развертывание

Проект настроен для развертывания на GitHub Pages.

### Сборка проекта

```bash
npm run build
```

Файлы сборки будут помещены в папку `dist`.

### Развертывание

Для деплоя на GitHub Pages выполните:

```bash
npm run deploy
```

## 🔌 Как использовать

1.  После запуска Excel перейдите на вкладку **Главная** (Home).
2.  В правой части ленты найдите группу **AI Ассистент**.
3.  Нажмите кнопку **AI Ассистент**, чтобы открыть панель задач.
4.  В открывшейся панели введите ваш API ключ (если требуется) и начните вводить команды, например:
    - "Посчитай сумму в столбце А"
    - "Отформатируй таблицу как зебру"
    - "Создай график продаж по месяцам"

## 📂 Структура проекта

- `manifest.xml`: Манифест надстройки для продакшена (GitHub Pages).
- `src/`: Исходный код.
  - `taskpane/`: Логика и UI панели задач.
  - `commands/`: Команды ленты (Ribbon).
  - `services/`: Сервисы для работы с AI и Excel API.
- `webpack.config.js`: Конфигурация сборщика.

## 📝 Лицензия

MIT
